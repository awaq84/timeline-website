User-agent: *
Allow: /

# app.js builds the whole page from these, so a crawler blocked from them renders
# an empty map and indexes nothing but the header. Listed before the Disallow
# because the most specific matching rule wins.
Allow: /data/index.json
Allow: /data/events/
Allow: /data/discover.js

# The rest of data/ is build intermediates and scripts/ is the offline Wikidata
# tooling -- neither is a page.
Disallow: /data/
Disallow: /scripts/

Sitemap: https://awaq84.github.io/timeline-website/sitemap.xml
