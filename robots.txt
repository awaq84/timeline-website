User-agent: *
Allow: /

# app.js builds the whole page from these two files, so a crawler blocked from
# them renders an empty map and indexes nothing but the header. Listed before the
# Disallow because the most specific matching rule wins. events.js is large, but
# a heavy crawl beats having no indexable content at all.
Allow: /data/events.js
Allow: /data/discover.js

# The rest of data/ is build intermediates and scripts/ is the offline Wikidata
# tooling -- neither is a page.
Disallow: /data/
Disallow: /scripts/

Sitemap: https://awaq84.github.io/timeline-website/sitemap.xml
